name: github-actions
on:
  push:
    branches:
      - '**'
    tags:
      - '**'
jobs:
  make-install:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - with:
          hexlet-id: ${{ secrets.HEXLET_ID }}      
      - run: make install
